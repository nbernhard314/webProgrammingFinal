const userData = require("./users");
const productData = require("./products");
const couponData = require("./coupons");

module.exports = {
  products: productData,
  users: userData,
  coupons: couponData
};
